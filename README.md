# Backlog Tamer

## Development

Run Ruff formatting and linting with:

```sh
make format
make lint
```

To check formatting without changing files:

```sh
make format-check
```

To apply safe lint fixes:

```sh
make lint-fix
```
